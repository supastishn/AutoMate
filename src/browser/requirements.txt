undetected-chromedriver>=3.5.0
selenium>=4.15.0
selenium-stealth>=1.0.6
# pyvirtualdisplay is optional — not needed on Termux/Android (headless mode used instead)
# Install it on desktop Linux for Xvfb support: pip install pyvirtualdisplay
